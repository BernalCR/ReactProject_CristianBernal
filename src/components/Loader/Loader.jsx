import { Orbit } from '@uiball/loaders'
import './Loader.css'

const Loader = () => {

    <Orbit size={25} speed={1.5} color="black"/>
    return (
        <div id='containerLoader' className='display_flex flex_center'>
            <div class="orbit"></div>
        </div>
    );
}

export default Loader;
